[project]
name = "PipeLineSystem"
version = "0.1.0"
description = "Video analytics pipeline — multi-process Streamer, Detector, and Displayer connected via IPC queues"
requires-python = ">=3.11"
dependencies = [
    "opencv-python>=4.8.0",
    "numpy>=1.24.0",
    "imutils>=0.5.4",
]
